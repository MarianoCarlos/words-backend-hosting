services:
    - type: web
      name: asl-backend
      env: python
      pythonVersion: 3.10.11
      buildCommand: pip install -r requirements.txt
      startCommand: python app.py
      envVars:
          - key: ENV
            value: production
